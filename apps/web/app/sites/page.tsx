export default function SitesPage() {
  return (
    <section className="panel">
      <h1>Sites</h1>
      <p>Use `POST /v1/sites` from this module to provision domains and runtime settings.</p>
    </section>
  );
}
